# Google Drive OAuth Credentials
# Get these from https://console.cloud.google.com/apis/credentials
#
# Setup:
# 1. Create OAuth 2.0 Client ID (type: Desktop app)
# 2. Set OAuth consent screen to "Testing" status
# 3. Add test users (your email) in OAuth consent screen
# 4. Copy Client ID and Client Secret here
# 5. Copy this file to .env and fill in real values
#
# Note: Desktop apps automatically allow loopback IPs (127.0.0.1)
# No redirect URI configuration needed in console

GOOGLE_CLIENT_ID=YOUR_CLIENT_ID.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=YOUR_CLIENT_SECRET
